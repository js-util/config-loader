{
	"name": "@js-util/config-loader",
	"version": "1.0.3",
	"description": "Quick and simple config loading, with sane defaults to get an application started",

	"keywords": [
		"config",
		"load",
		"hjson",
		"json"
	],

	"main": "./src/ConfigLoader.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	
	"author": "picocreator",
	"license": "MIT",
	"private": false,

	"dependencies": {
		"deepmerge": "^4.2.2",
		"fs-extra": "^8.1.0",
		"hjson": "^3.2.1",
		"@js-util/fetch-nested-value": "^1.0.2"
	}
}
